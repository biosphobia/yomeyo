{
  "name": "@yomeyo/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node ../../scripts/ensure-dict.mjs",
    "dev": "vite",
    "prebuild": "node ../../scripts/ensure-dict.mjs",
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@yomeyo/core": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
