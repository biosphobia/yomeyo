{
  "name": "@yomeyo/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node ../../scripts/ensure-dict.mjs && node ../../scripts/ensure-kanji.mjs",
    "dev": "vite",
    "prebuild": "node ../../scripts/ensure-dict.mjs && node ../../scripts/ensure-kanji.mjs",
    "build": "tsc --noEmit -p tsconfig.json && vite build && node ../../scripts/stamp-sw.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@yomeyo/core": "*",
    "firebase": "^12.16.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
