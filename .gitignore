node_modules/
dist/
*.tsbuildinfo
packages/*/dist/

# downloaded dictionary sources (rebuild with npm run build-dict)
data/jmdict-*.json
data/jmdict-*.zip

# built dictionaries are large; the committed seed lives in data/
apps/web/public/dict/
apps/extension/public/dict/

# sync server data
yomeyo-data.json
*.tmp

# built kanji data (rebuild with npm run build-kanji; seed is in data/)
apps/web/public/kanji/index.json
apps/web/public/kanji/strokes/
data/kanjidic2-*.json
data/kanjidic2-*.zip
data/kanjivg*

# macOS Finder leaves these in every folder it opens.
.DS_Store
