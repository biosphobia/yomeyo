{
  "name": "yomeyo",
  "private": true,
  "version": "0.1.0",
  "description": "Japanese sentence-mining and spaced-repetition vocabulary app: tap words in your browser, review them as daily flashcards, synced across devices.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w @yomeyo/core && npm run build -w @yomeyo/web && npm run build -w @yomeyo/extension",
    "test": "npm run test -w @yomeyo/core",
    "dev:web": "npm run dev -w @yomeyo/web",
    "sync-server": "node apps/sync-server/server.mjs",
    "build-dict": "node scripts/build-dict.mjs",
    "build-kanji": "node scripts/build-kanji.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
